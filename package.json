{
  "name": "forma-ai",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace=@forma-ai/client",
    "dev:server": "npm run dev --workspace=@forma-ai/server",
    "lint": "npm run lint --workspaces --if-present"
  }
}
